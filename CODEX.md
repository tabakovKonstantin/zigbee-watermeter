# Codex Entry Point

Read `AGENTS.md` before changing or operating this project. It contains the durable repository rules and points to the task-specific skills under `.agents/skills/`.

Use `python3 tools/watermeter_harness.py` for toolchain discovery, builds, tests, serial-port selection, flashing, monitoring, OTA packaging, partition erasure, and Zigbee2MQTT audit/deploy. Do not copy machine-specific ESP-IDF paths, serial ports, or partition offsets into this file.
